<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--Theme for TweetActivity, linked in the manifest.-->
    <!--http://stackoverflow.com/questions/16007750/how-to-create-custom-popupwindow-from-intent-call-->
    <style name="TweetActivityTheme" parent="android:style/Theme.Dialog">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <!--Theme for progress dialog. https://developer.android.com/guide/topics/ui/themes.html-->
    <style name="ProgressDialogTheme" parent="android:Theme.Material.Dialog">
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <style name="LinearProgress" parent="Theme.AppCompat.Light">
        <item name="colorAccent">#009688</item>
        <item name="android:progressBackgroundTint">#B2DFDB</item>
    </style>

</resources>
